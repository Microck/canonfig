diff --git a/src/agent/agent-resolution.service.ts b/src/agent/agent-resolution.service.ts
index 7e19792..5d86257 100644
--- a/src/agent/agent-resolution.service.ts
+++ b/src/agent/agent-resolution.service.ts
@@ -2139,10 +2139,15 @@ const packageManagerPolicy = (
     command === "tool"
     && unambiguous[commandIndex! + 1]?.toLowerCase() === "install"
   ) {
-    if (hasNonCanonicalBinaryOption(unambiguous)) {
-      return { kind: "denied" };
-    }
-    const canonical = "--only-binary=:all:";
+    // `--only-binary` belongs to the pip interface, not `uv tool install`.
+    // Keep the no-source-build guarantee without accepting conflicting flags.
+    if (unambiguous.some((argument) => {
+      const lower = argument.toLowerCase();
+      const name = lower.split("=", 1)[0]!;
+      return ["--only-binary", "--no-binary", "--no-binary-package", "--build"].includes(name)
+        || lower.startsWith("--no-build=");
+    })) return { kind: "denied" };
+    const canonical = "--no-build";
     if (
       hasDisabledOption(unambiguous, "--no-config")
       || hasSeparateOptionValue(unambiguous, "--no-config")
@@ -2153,7 +2158,7 @@ const packageManagerPolicy = (
       kind: "scripts-disabled",
       arguments: [
         ...(
-          hasOnlyBinaryAll(unambiguous)
+          unambiguous.includes(canonical)
             ? arguments_
             : [...arguments_, canonical]
         ),
diff --git a/src/profile/tool-catalog.ts b/src/profile/tool-catalog.ts
index 5e6bce3..a9e2415 100644
--- a/src/profile/tool-catalog.ts
+++ b/src/profile/tool-catalog.ts
@@ -346,7 +346,7 @@ const recipeFromPackage = (
             "tool",
             "install",
             specification,
-            "--only-binary=:all:",
+            "--no-build",
             "--no-config",
             `--default-index=${index}`,
           ]
diff --git a/src/synchronization/resource-executors.ts b/src/synchronization/resource-executors.ts
index a79ffb4..0b6df1b 100644
--- a/src/synchronization/resource-executors.ts
+++ b/src/synchronization/resource-executors.ts
@@ -1555,7 +1555,7 @@ const installInvocation = (
         "tool",
         "install",
         version === undefined ? packageName : `${packageName}==${version}`,
-        ...(buildPolicy.mode === "scripts-disabled" ? ["--only-binary=:all:"] : []),
+        ...(buildPolicy.mode === "scripts-disabled" ? ["--no-build"] : []),
         "--no-config",
         `--default-index=${pythonIndex!}`,
       ]
diff --git a/tests/agent-resolution.test.ts b/tests/agent-resolution.test.ts
index f37337f..e5830d0 100644
--- a/tests/agent-resolution.test.ts
+++ b/tests/agent-resolution.test.ts
@@ -1221,8 +1221,10 @@ describe("agent resolution", () => {
     ]],
     ["bun install", "bun", ["--cwd", directory, "install"], ["--ignore-scripts"]],
     ["uv tool install", "uv", ["tool", "install", "tool==1.2.3"], [
-      "--only-binary=:all:",
+      "--no-build",
     ]],
+    ["uv tool install with explicit no-build", "uv", ["tool", "install", "tool==1.2.3", "--no-build"], []],
+    ["uv tool install with leading no-build", "uv", ["--no-build", "tool", "install", "tool==1.2.3"], []],
     ["uv pip install", "uv", ["pip", "install", "tool==1.2.3"], [
       "--only-binary=:all:",
     ]],
@@ -1286,7 +1288,7 @@ describe("agent resolution", () => {
       "-C",
       "build.mode=release",
     ], [
-      "--only-binary=:all:",
+      "--no-build",
       "--no-config",
       "--default-index=https://packages.example.test",
     ]],
@@ -1971,6 +1973,10 @@ describe("agent resolution", () => {
       "tool",
       "--no-binary=:all:",
     ]],
+    ["uv unsupported pip binary flag", "uv", ["tool", "install", "tool", "--only-binary=:all:"]],
+    ["uv disabled no-build", "uv", ["tool", "install", "tool", "--no-build=false"]],
+    ["uv conflicting build request", "uv", ["tool", "install", "tool", "--build"]],
+    ["uv package source build", "uv", ["tool", "install", "tool", "--no-binary-package=tool"]],
     ["uv noncanonical binary mode", "uv", [
       "tool",
       "install",
@@ -2707,7 +2713,7 @@ writeFileSync(${JSON.stringify(marker)}, JSON.stringify({
           "tool",
           "install",
           "tool==1.2.3",
-          "--only-binary=:all:",
+          "--no-build",
           "--no-config",
           `--default-index=${index}`,
         ],
@@ -2821,7 +2827,7 @@ require("node:fs").writeFileSync(${JSON.stringify(marker)}, "spawned");
           "tool==1.2.3",
           "-C",
           "build.mode=release",
-          "--only-binary=:all:",
+          "--no-build",
           "--no-config",
           "--default-index=https://packages.example.test",
         ],
diff --git a/tests/discovery.test.ts b/tests/discovery.test.ts
index 9d205fd..82a5172 100644
--- a/tests/discovery.test.ts
+++ b/tests/discovery.test.ts
@@ -203,7 +203,7 @@ describe("profile discovery", () => {
           "tool",
           "install",
           "binary-tool==1.0.0",
-          "--only-binary=:all:",
+          "--no-build",
           "--no-config",
           "--default-index=https://pypi.org/simple",
         ],
@@ -414,7 +414,7 @@ describe("profile discovery", () => {
         "tool",
         "install",
         "uv-tool==5.0.0",
-        "--only-binary=:all:",
+        "--no-build",
         "--no-config",
         "--default-index=https://pypi.org/simple",
       ],
diff --git a/tests/integration/synchronization-run.test.ts b/tests/integration/synchronization-run.test.ts
index 6b3393b..b0b9636 100644
--- a/tests/integration/synchronization-run.test.ts
+++ b/tests/integration/synchronization-run.test.ts
@@ -1133,7 +1133,7 @@ if (process.argv.slice(2).some((value) =>
         "tool",
         "install",
         "uv-tool==1.2.3",
-        "--only-binary=:all:",
+        "--no-build",
         "--no-config",
         `--default-index=${expectedIndex}`,
       ]);
@@ -3385,7 +3385,7 @@ if (process.argv.slice(2).some((value) =>
       "tool",
       "install",
       "tool==1.2.3",
-      "--only-binary=:all:",
+      "--no-build",
       "--no-config",
       "--default-index=https://pypi.org/simple",
     ]],
diff --git a/website/content/docs/explanation/tools-and-recipes.mdx b/website/content/docs/explanation/tools-and-recipes.mdx
index e4a7519..1aa0e3d 100644
--- a/website/content/docs/explanation/tools-and-recipes.mdx
+++ b/website/content/docs/explanation/tools-and-recipes.mdx
@@ -91,7 +91,8 @@ HTTPS simple-index URL with `reviewedBy` and `reviewedAt`. Private simple-index
 subpaths remain intact. Without that policy, Canonfig uses the fixed
 `https://pypi.org/simple` index. Deterministic uv actions use `--no-config`,
 the full approved `--default-index`, exact package versions, and
-`--only-binary=:all:` under the default build policy.
+`uv tool install --no-build` under the default build policy. `--only-binary`
+belongs to the pip interface and is not a valid `uv tool install` option.
 
 ## npm configuration isolation
 
