name: Temporary process-input refactor

on:
  pull_request:

permissions:
  contents: write

jobs:
  apply:
    if: github.head_ref == 'feat/secure-secret-sync'
    runs-on: ubuntu-latest
    steps:
      - name: Check out feature branch
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: feat/secure-secret-sync
          fetch-depth: 0

      - name: Apply reviewed patch
        shell: bash
        run: |
          cat > /tmp/refactor.patch <<'PATCH'
          diff --git a/src/machine/linux.layer.ts b/src/machine/linux.layer.ts
          index 2315324..b25864d 100644
          --- a/src/machine/linux.layer.ts
          +++ b/src/machine/linux.layer.ts
          @@ -86,6 +86,7 @@ import type {
           const decode = Schema.decodeUnknownSync;
           const defaultDirectoryMode = 0o700;
           const defaultFileMode = 0o600;
          +const maximumProcessInputBytes = 64 * 1024;
           
           class ProcessTimeoutSignal extends Error {}
           class ProcessOutputLimitSignal extends Error {}
          @@ -806,6 +807,15 @@ const runBoundedProcess = (
                   maximumOutputBytes: invocation.maximumOutputBytes,
                 });
               }
          +    if (
          +      invocation.standardInput !== undefined
          +      && invocation.standardInput.byteLength > maximumProcessInputBytes
          +    ) {
          +      return yield* new ProcessStartError({
          +        executable,
          +        message: `standard input exceeds ${maximumProcessInputBytes} bytes`,
          +      });
          +    }
               return yield* Effect.tryPromise({
                 try: (signal) => new Promise<ProcessResult>((resolveProcess, rejectProcess) => {
                   const output: Array<Buffer> = [];
          @@ -821,7 +831,11 @@ const runBoundedProcess = (
                       invocation.environmentUnsetPrefixes ?? [],
                     ),
                     shell: false,
          -          stdio: ["ignore", "pipe", "pipe"],
          +          stdio: [
          +            invocation.standardInput === undefined ? "ignore" : "pipe",
          +            "pipe",
          +            "pipe",
          +          ],
                     detached: process.platform !== "win32",
                   });
                   const terminate = (reason: Error): void => {
          @@ -842,6 +856,12 @@ const runBoundedProcess = (
                   child.once("error", (cause) => {
                     failure = new ProcessStartSignal(messageOf(cause));
                   });
          +        if (invocation.standardInput !== undefined && child.stdin !== null) {
          +          child.stdin.once("error", (cause) => {
          +            terminate(new ProcessStartSignal(messageOf(cause)));
          +          });
          +          child.stdin.end(invocation.standardInput);
          +        }
                   const timer = setTimeout(
                     () => terminate(new ProcessTimeoutSignal()),
                     invocation.timeoutMilliseconds,
          diff --git a/src/secrets/native-secret-store.ts b/src/secrets/native-secret-store.ts
          index be174ff..8b7a3cf 100644
          --- a/src/secrets/native-secret-store.ts
          +++ b/src/secrets/native-secret-store.ts
          @@ -1,4 +1,3 @@
          -import { spawn } from "node:child_process";
           import { createHash } from "node:crypto";
           import { win32 } from "node:path";
           
          @@ -17,7 +16,6 @@ import type {
             StoreCredentialInput,
           } from "../machine/machine-state.types.ts";
           
          -const maximumCredentialInputBytes = 64 * 1024;
           const maximumCredentialOutputBytes = 1024 * 1024;
           const credentialTimeoutMilliseconds = 5_000;
           const decode = Schema.decodeUnknownSync;
          @@ -34,18 +32,11 @@ export interface NativeCredentialWriteCommand {
           export interface NativeSecretStoreLayerOptions {
             readonly environment?: NodeJS.ProcessEnv | undefined;
             readonly runCommand?: ((
          +    machine: MachineState["Service"],
               command: NativeCredentialWriteCommand,
             ) => Effect.Effect<number | null, MachineStateError>) | undefined;
           }
           
          -const commandEnvironment = (
          -  additions: ReadonlyArray<ProcessEnvironmentEntry>,
          -): NodeJS.ProcessEnv => {
          -  const environment: NodeJS.ProcessEnv = { ...process.env };
          -  for (const entry of additions) environment[entry.name] = entry.value;
          -  return environment;
          -};
          -
           const failure = (
             provider: NativeCredentialWriteCommand["provider"],
           ): HumanActionRequiredError =>
          @@ -60,59 +51,23 @@ const failure = (
               });
           
           const runNativeCredentialCommand = (
          +  machine: MachineState["Service"],
             command: NativeCredentialWriteCommand,
          -): Effect.Effect<number | null, MachineStateError> => {
          -  if (command.standardInput.byteLength > maximumCredentialInputBytes) {
          -    return Effect.fail(new CredentialStorageError({
          -      operation: "store credential",
          -      reference: command.provider,
          -      message: "credential input exceeds the native-store size limit",
          -    }));
          -  }
          -
          -  return Effect.tryPromise({
          -    try: (signal) =>
          -      new Promise<number | null>((resolveCommand, rejectCommand) => {
          -        const child = spawn(command.executable, [...command.arguments], {
          -          env: commandEnvironment(command.environment),
          -          shell: false,
          -          stdio: ["pipe", "pipe", "pipe"],
          -          windowsHide: true,
          -        });
          -        let outputBytes = 0;
          -        let failed = false;
          -        const fail = (cause: unknown): void => {
          -          if (failed) return;
          -          failed = true;
          -          child.kill();
          -          rejectCommand(cause);
          -        };
          -        const capture = (chunk: Buffer): void => {
          -          outputBytes += chunk.byteLength;
          -          if (outputBytes > maximumCredentialOutputBytes) {
          -            fail(new Error("native credential command exceeded its output limit"));
          -          }
          -        };
          -        child.stdout.on("data", capture);
          -        child.stderr.on("data", capture);
          -        child.once("error", fail);
          -        child.stdin.once("error", fail);
          -        const timer = setTimeout(
          -          () => fail(new Error("native credential command timed out")),
          -          credentialTimeoutMilliseconds,
          -        );
          -        const abort = (): void => fail(new Error("native credential command aborted"));
          -        signal.addEventListener("abort", abort, { once: true });
          -        child.once("close", (exitCode) => {
          -          clearTimeout(timer);
          -          signal.removeEventListener("abort", abort);
          -          if (!failed) resolveCommand(exitCode);
          -        });
          -        child.stdin.end(command.standardInput);
          -      }),
          -    catch: () => failure(command.provider),
          +): Effect.Effect<number | null, MachineStateError> =>
          +  Effect.gen(function*() {
          +    const executable = yield* machine.normalizePath({
          +      path: command.executable,
          +    });
          +    const result = yield* machine.runProcess({
          +      executable,
          +      arguments: command.arguments,
          +      environment: command.environment,
          +      standardInput: command.standardInput,
          +      timeoutMilliseconds: credentialTimeoutMilliseconds,
          +      maximumOutputBytes: maximumCredentialOutputBytes,
          +    });
          +    return result.exitCode;
             });
          -};
           
           export const nativeCredentialWriteCommand = (
             capability: Extract<CredentialStorageCapability, {
          @@ -203,6 +158,7 @@ export const nativeSecretStoreLayer = (
                     );
                     if (command === undefined) return yield* machine.storeCredential(input);
                     const exitCode = yield* (options.runCommand ?? runNativeCredentialCommand)(
          +            machine,
                       command,
                     );
                     if (exitCode !== 0) return yield* failure(command.provider);
          diff --git a/tests/integration/secret-cleanup-retry.test.ts b/tests/integration/secret-cleanup-retry.test.ts
          index 5157e8c..27ebf4f 100644
          --- a/tests/integration/secret-cleanup-retry.test.ts
          +++ b/tests/integration/secret-cleanup-retry.test.ts
          @@ -1,13 +1,14 @@
           import { mkdtempSync, rmSync } from "node:fs";
           import { readFile } from "node:fs/promises";
           import { tmpdir } from "node:os";
          -import { join } from "node:path";
          +import { join, win32 } from "node:path";
           
           import { Effect, Layer, Redacted, Schema } from "effect";
           import { describe, expect, it } from "vitest";
           
           import { CredentialReference } from "../../src/domain/brand.ts";
           import { linuxMachineStateLayer } from "../../src/machine/linux.layer.ts";
          +import { windowsMachineStateLayer } from "../../src/machine/windows.layer.ts";
           import { CredentialStorageError } from "../../src/machine/machine-state.errors.ts";
           import { MachineState } from "../../src/machine/machine-state.service.ts";
           import { nativeCredentialWriteCommand } from "../../src/secrets/native-secret-store.ts";
          @@ -119,6 +120,85 @@ describe("shared-secret cleanup retry", () => {
               }
             });
           
          +  it("passes bounded standard input through the machine process boundary", async () => {
          +    const root = mkdtempSync(join(tmpdir(), "canonfig-process-input-"));
          +    const layer = linuxMachineStateLayer({
          +      environment: [
          +        { name: "HOME", value: join(root, "home") },
          +        { name: "PATH", value: process.env.PATH ?? "" },
          +      ],
          +      credentialPolicy: {
          +        kind: "local-file",
          +        path: join(root, "credentials"),
          +      },
          +    });
          +    const input = "bounded-standard-input";
          +
          +    try {
          +      const result = await Effect.runPromise(
          +        Effect.gen(function*() {
          +          const machine = yield* MachineState;
          +          const executable = yield* machine.normalizePath({
          +            path: process.execPath,
          +          });
          +          return yield* machine.runProcess({
          +            executable,
          +            arguments: [
          +              "-e",
          +              "process.stdin.pipe(process.stdout)",
          +            ],
          +            standardInput: new TextEncoder().encode(input),
          +            timeoutMilliseconds: 5_000,
          +            maximumOutputBytes: 4_096,
          +          });
          +        }).pipe(Effect.provide(layer)),
          +      );
          +
          +      expect(result.exitCode).toBe(0);
          +      expect(Buffer.from(result.standardOutput).toString("utf8")).toBe(input);
          +    } finally {
          +      rmSync(root, { recursive: true, force: true });
          +    }
          +  });
          +
          +  it("rejects oversized standard input before starting a process", async () => {
          +    const root = mkdtempSync(join(tmpdir(), "canonfig-process-limit-"));
          +    const layer = linuxMachineStateLayer({
          +      environment: [
          +        { name: "HOME", value: join(root, "home") },
          +        { name: "PATH", value: process.env.PATH ?? "" },
          +      ],
          +      credentialPolicy: {
          +        kind: "local-file",
          +        path: join(root, "credentials"),
          +      },
          +    });
          +
          +    try {
          +      await expect(
          +        Effect.runPromise(
          +          Effect.gen(function*() {
          +            const machine = yield* MachineState;
          +            const executable = yield* machine.normalizePath({
          +              path: process.execPath,
          +            });
          +            return yield* machine.runProcess({
          +              executable,
          +              arguments: ["-e", "process.exit(0)"],
          +              standardInput: new Uint8Array(64 * 1024 + 1),
          +              timeoutMilliseconds: 5_000,
          +              maximumOutputBytes: 4_096,
          +            });
          +          }).pipe(Effect.provide(layer)),
          +        ),
          +      ).rejects.toMatchObject({
          +        _tag: "ProcessStartError",
          +      });
          +    } finally {
          +      rmSync(root, { recursive: true, force: true });
          +    }
          +  });
          +
             it("keeps macOS secret values out of arguments and environment", () => {
               const secret = "mac-secret-value-not-process-metadata";
               const command = nativeCredentialWriteCommand(
          @@ -164,4 +244,50 @@ describe("shared-secret cleanup retry", () => {
               expect(script).toContain("[System.Text.UTF8Encoding]::new($false)");
               expect(script).toContain("[Console]::In.ReadToEnd()");
             });
          +
          +  it.runIf(process.platform === "win32")(
          +    "decodes multibyte Windows standard input as UTF-8",
          +    async () => {
          +      const secret = "é🔐-windows-round-trip";
          +      const layer = windowsMachineStateLayer();
          +      const powershell = win32.join(
          +        process.env.SystemRoot ?? "C:\\Windows",
          +        "System32",
          +        "WindowsPowerShell",
          +        "v1.0",
          +        "powershell.exe",
          +      );
          +      const script = [
          +        "$ErrorActionPreference='Stop'",
          +        "[Console]::InputEncoding=[System.Text.UTF8Encoding]::new($false)",
          +        "[Console]::OutputEncoding=[System.Text.UTF8Encoding]::new($false)",
          +        "$value=[Console]::In.ReadToEnd()",
          +        "[Console]::Out.Write($value)",
          +      ].join(";");
          +
          +      const result = await Effect.runPromise(
          +        Effect.gen(function*() {
          +          const machine = yield* MachineState;
          +          const executable = yield* machine.normalizePath({ path: powershell });
          +          return yield* machine.runProcess({
          +            executable,
          +            arguments: [
          +              "-NoLogo",
          +              "-NoProfile",
          +              "-NonInteractive",
          +              "-Command",
          +              script,
          +            ],
          +            standardInput: new TextEncoder().encode(secret),
          +            timeoutMilliseconds: 5_000,
          +            maximumOutputBytes: 4_096,
          +          });
          +        }).pipe(Effect.provide(layer)),
          +      );
          +
          +      expect(result.exitCode).toBe(0);
          +      expect(Buffer.from(result.standardOutput).toString("utf8")).toBe(secret);
          +      expect(Buffer.from(result.standardError).toString("utf8")).toBe("");
          +    },
          +  );
           });
          PATCH

          git apply --check /tmp/refactor.patch
          git apply /tmp/refactor.patch
          git config user.name "Microck"
          git config user.email "contact@micr.dev"
          git add src/machine/linux.layer.ts src/secrets/native-secret-store.ts tests/integration/secret-cleanup-retry.test.ts
          git commit -m "refactor: use bounded machine stdin for native secrets"
          git push origin HEAD:feat/secure-secret-sync
